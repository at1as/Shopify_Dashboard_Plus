source 'https://rubygems.org'

# Set ruby version for Heroku without affecting versions used for TravisCI
ruby ENV['CUSTOM_RUBY_VERSION'] if ENV['CUSTOM_RUBY_VERSION']

# Gems specified in shopify-dashboard-plus.gemspec
gemspec
